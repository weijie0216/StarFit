<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.starfit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".Water.RecordWater"></activity>
        <activity android:name=".CreateNewAccount2"></activity>
        <activity android:name=".MapsActivity"></activity>
        <activity android:name=".RecordActivity"></activity>
        <activity android:name=".FoodData.AddActivity"></activity>
        <activity android:name=".FoodData.FoodMainActivity"></activity>
        <activity android:name="com.example.starfit.HomeFragment"></activity>



        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateNewAccount1"
            android:label="Create New Account" />
    </application>

</manifest>